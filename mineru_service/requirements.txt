# FastAPI wrapper dependencies only.
# MinerU itself is installed in the Dockerfile (separate layer for caching).
fastapi==0.109.0
uvicorn[standard]==0.27.0
python-multipart==0.0.6
PyMuPDF>=1.24,<1.27
httpx>=0.27.0
