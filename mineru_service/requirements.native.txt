# Native Linux 4090 runtime for mineru_service.
# Use with:
#   python3 -m venv .venv
#   source .venv/bin/activate
#   pip install -U pip
#   pip install -r requirements.native.txt

-r requirements.txt
mineru[core]>=2.7.0
